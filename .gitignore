# Whitelist core files
!.gitignore
!.gitattributes
!.github/
!.github/**
!README.md
!RUV_DOWNLOADS_COMPLETE_GUIDE.md
!ruv_world.sh

# Whitelist directory structure and allow artifacts (managed via Git LFS)
!manifests/
!manifests/*.txt
!scripts/
!scripts/*.sh
!lib/
!lib/*.sh
!artifacts/
!artifacts/archives/
!artifacts/archives/**/*.tar.gz
!artifacts/archives/**/*.tgz
!artifacts/archives/**/*.crate

# Ignore huge artifact directories (they are downloaded on the fly)
artifacts/repos/
artifacts/gists/
artifacts/*/extracted/

# Ignore logs and temporary files
logs/*.log
logs/**/*.log
cache/
*.tmp


# Build artifacts and dependencies in extracted content (not archives)
node_modules/**
**/target/
**/dist/
**/.git/

# Ignore metadata files that we don't want tracked (if any, previous file ignored some json/md)
# Re-adding specific whitelists for metadata if they were previously strictly controlled
# The user wants "all full files", so we err on the side of including everything.
# We will keep the specific script ignores if they were important?
# Previous file: !crates/*.sh, !crates/*.dynamic.txt - These are now covered by !crates/**


# OS files
.DS_Store
.env
*.log

# Node/npm cache - explicitly ignore these recursively
node_modules/**
.npm/**

# Python virtual environments
**/venv/**

# Tool configurations
.claude-flow
.claude/
