# Whitelist core files
!.gitignore
!.github/
!.github/**
!README.md
!RUV_DOWNLOADS_COMPLETE_GUIDE.md
!ruv_world.sh

# Whitelist directory structure (but ignore contents by default)
!manifests/
!manifests/*.txt
!scripts/
!scripts/*.sh
!lib/
!lib/*.sh
!artifacts/
!artifacts/index.json
!artifacts/**/.gitkeep

# Ignore large downloaded artifacts (Archives and Extracted code)
# These should be managed by the scripts, not committed to Git (Standard Repo)
# If a full backup in Git is desired, these can be whitelisted.
artifacts/crates/archives/
artifacts/crates/extracted/
artifacts/npm/archives/
artifacts/npm/extracted/
artifacts/repos/*
artifacts/gists/*

# Ignore logs
logs/*.log
cache/


# ... But re-ignore build artifacts and large files
crates/**/target/
crates/**/*.crate
npmjs/**/node_modules/
npmjs/**/*.tgz
github/**/node_modules/
github/**/target/
github/**/dist/
github/**/.git/

# Ignore metadata files that we don't want tracked (if any, previous file ignored some json/md)
# Re-adding specific whitelists for metadata if they were previously strictly controlled
# The user wants "all full files", so we err on the side of including everything.
# We will keep the specific script ignores if they were important?
# Previous file: !crates/*.sh, !crates/*.dynamic.txt - These are now covered by !crates/**


# OS files
.DS_Store
.env
*.log

# Node/npm cache - explicitly ignore these recursively
node_modules/**
.npm/**

# Python virtual environments
**/venv/**
**/.venv/**
