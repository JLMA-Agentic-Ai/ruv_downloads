# Git LFS Configuration for RUV Downloads
# Only compressed archives in the unified archives directory should be tracked via LFS

# Target specific compressed formats in the archives hierarchy
artifacts/archives/**/*.crate filter=lfs diff=lfs merge=lfs -text
artifacts/archives/**/*.tgz filter=lfs diff=lfs merge=lfs -text
artifacts/archives/**/*.tar.gz filter=lfs diff=lfs merge=lfs -text

# Explicitly exempt gists from LFS (they are stored as uncompressed text)
artifacts/archives/github/gists/** !filter !diff !merge

# Explicitly exempt extracted content and active repos
artifacts/crates/extracted/** !filter !diff !merge
artifacts/npm/extracted/** !filter !diff !merge
artifacts/repos/** !filter !diff !merge

# Metadata and small files should always be plain text
*.json !filter !diff !merge
*.txt !filter !diff !merge
*.md !filter !diff !merge
*.sh !filter !diff !merge
*.gitkeep !filter !diff !merge
