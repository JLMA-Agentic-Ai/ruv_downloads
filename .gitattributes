# Git LFS Configuration for RUV Downloads
# This ensures large artifact files are stored efficiently

# Rust crate archives
*.crate filter=lfs diff=lfs merge=lfs -text

# Compressed Archives (Universal)
artifacts/archives/** filter=lfs diff=lfs merge=lfs -text
# Exempt Gists from LFS (Store as Text)
artifacts/archives/github/gists/** !filter !diff !merge
*.tgz filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text

# Git repositories and gists (entire directories) -> IGNORED (Removed from LFS)
# artifacts/repos/** filter=lfs diff=lfs merge=lfs -text
# artifacts/gists/** filter=lfs diff=lfs merge=lfs -text

# Extracted content -> IGNORED (Removed from LFS)
# artifacts/crates/extracted/** filter=lfs diff=lfs merge=lfs -text
# artifacts/crates/legacy/** filter=lfs diff=lfs merge=lfs -text
# artifacts/npm/extracted/** filter=lfs diff=lfs merge=lfs -text
# artifacts/npm/legacy/** filter=lfs diff=lfs merge=lfs -text

# Exclude metadata and small files from LFS
*.json !filter !diff !merge
*.txt !filter !diff !merge
*.md !filter !diff !merge
*.sh !filter !diff !merge
*.gitkeep !filter !diff !merge
